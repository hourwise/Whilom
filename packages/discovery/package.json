{
  "name": "@whilom/discovery",
  "version": "0.0.0",
  "private": true,
  "description": "Shared, platform-neutral Whilom discovery vocabulary, query contracts and read adapter.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "@whilom/database": "workspace:*",
    "@whilom/domain": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
