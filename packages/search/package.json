{
  "name": "@heritage/search",
  "version": "0.0.0",
  "private": true,
  "description": "Search query construction shared by web + mobile. PostGIS/text now; room for semantic search later (spec §37).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@heritage/domain": "workspace:*",
    "@heritage/validation": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.1.1",
    "rimraf": "^6.0.1"
  }
}
