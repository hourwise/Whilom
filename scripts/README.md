# scripts/

Repo automation and one-off maintenance scripts (data backfills, migration
helpers, seed generators). Keep anything touching privileged credentials here or
in `ingestion/`, never in an app.
