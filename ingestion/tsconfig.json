{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["sources/**/*.ts", "transforms/**/*.ts", "matching/**/*.ts", "enrichment/**/*.ts", "pipeline/**/*.ts", "scale/**/*.ts"],
  "exclude": ["node_modules", "dist", "**/*.test.ts"]
}
