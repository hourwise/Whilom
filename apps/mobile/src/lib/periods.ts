export {
  PERIODS as MOBILE_PERIODS,
  formatPeriodSpan,
  formatYear as formatHistoricalYear,
  periodById,
  TIME_MODE_LABELS,
  TIME_MODES,
} from '@whilom/discovery';
export type { Period, TimeMode as MobileTimeMode } from '@whilom/discovery';
export { TIME_MODE_OPTIONS } from './time-mode-options';

